import { Subjects } from './subjects';
export interface ElementUpdatedEvent {
    subject: Subjects.ElementUpdated;
    data: {
        id: string;
        version: number;
        title: string;
        price: number;
        userId: string;
    };
}
